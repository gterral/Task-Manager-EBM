{% extends "EBMKMBundle::bundleLayout.html.twig" %}

{% block content_body %}
    <a href="{{ path('ebmkm_forum_homepage') }}">Retour au forum</a>
    <h2>{{ topic.title }}</h2>

    {% for post in topic.posts %}
        <div class="well"><strong>{{ post.author.username }}</strong> -- {{ post.date | date('d-M-Y H:i:s') }}<p>{{ post.content }}</p></div>

    {% endfor %}

    <a href="{{ path('ebmkm_forum_answer_topic', {'id': topic.id}) }}">Répondre</a>
{% endblock %}